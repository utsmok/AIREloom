# Define relative paths for each entity (e.g., RESEARCH_PRODUCTS = "researchProducts").
# For each entity type, define sets or dictionaries containing:
# VALID_FILTERS: All filter parameter names allowed for that entity (from filtering.md).
# VALID_SORT_FIELDS: All field names allowed in the sortBy parameter (from filtering.md or sorting_and_paging.md).
